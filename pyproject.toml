[project]
name = "bebo-safe-api"
version = "1.0.0"
description = "BEBO Safe API: terminal web controlada para Render Free"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.115,<1",
  "uvicorn[standard]>=0.30,<1",
  "pydantic>=2.8,<3",
  "asyncpg>=0.29,<1",
]

[tool.uv]
dev-dependencies = [
  "httpx>=0.27,<1",
  "pytest>=8,<9",
]
